/**
 * Frozen re-export thunk — superseded by the `dynamodb-toolkit/lambda`
 * subpath export (dynamodb-toolkit 3.8.0+). New code should import
 * `dynamodb-toolkit/lambda` directly; the API is identical.
 */
export * from 'dynamodb-toolkit/lambda';
